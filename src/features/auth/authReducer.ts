// import { createAction, createReducer } from '@reduxjs/toolkit';

// const user: UserType = {
//   username: '',
// };

// const login = createAction('auth/login');
// const logout = createAction('auth/logout');
// const updateUsername = createAction<string>('auth/updateName');

// const authReducer = createReducer(user, (builder) => {
//   builder
//     .addCase(login, (state) => {
//       state.username = 'Vimal Ram K';
//     })
//     .addCase(logout, (state) => {
//       state.username = '';
//     })
//     .addCase(updateUsername, (state, action) => {
//       state.username = action.payload;
//     });
// });

// export { login, logout, updateUsername };
